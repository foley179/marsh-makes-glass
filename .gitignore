# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (contains Supabase project URL/key for local dev)
.env
.env.*
!.env.example

# Supabase - schema/migrations/functions/templates are applied directly in
# the Supabase dashboard/CLI and kept out of this public repo on purpose
# (not because they contain secrets - they don't - just to avoid
# publishing the exact DB/business logic design).
supabase/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
